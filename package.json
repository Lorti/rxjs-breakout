{
  "name": "rxjs-breakout",
  "version": "1.0.0",
  "description": "RxJS Breakout",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify main.js -o index.js -t [ babelify --presets [ es2015 ] ]",
    "budo": "budo main.js:index.js --live -- -t [ babelify --presets [ es2015 ] ]"
  },
  "author": "office@manuelwieser.com",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "budo": "^8.1.0",
    "rx": "^4.1.0"
  }
}
